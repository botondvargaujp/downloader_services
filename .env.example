# StatsBomb API credentials for the weekly pipeline.
# Copy this file to `.env` (same directory) and fill in real values.
# `.env` is git-ignored and must never be committed.
STATSBOMB_USER=your_statsbomb_user
STATSBOMB_PASSWD=your_statsbomb_password
